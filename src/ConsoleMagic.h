#pragma once

#include <string>


const void outputLine(const uint8_t n = 63);
const void outputString(const std::string& text);
const void outputShader(const std::string& vertex_shader, const std::string& fragment_shader);